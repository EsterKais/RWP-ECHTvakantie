.slider
  .container
    h1 Ontdek echt bijzondere verblijven voor een weekendje weg of vakantie.
    h2 Kleinschalig, persoonlijk en authentiek. Voor een echt vakantiegevoel.
    
     
      
.container
  .themes
    .grid
      - @themes.each do |theme|  
        = link_to theme do
          .theme-column style="background: linear-gradient(rgba(0, 0, 0, 0.5),rgba(0, 0, 0, 0.5)), url(#{theme.tphotos[0].image}); background-size: cover;"
            = theme.name
            br
            = link_to 'Edit', edit_theme_path(theme), class: "admin"
            = link_to 'Destroy', theme, method: :delete, data: {confirm: 'Are you sure?'}, class: "admin"
    = link_to new_theme_path do
      .new-theme
        | New theme
          
